pipeline {
    agent any

    stages {
        stage('Start Build') {
            steps {
                echo '开始构建...'
            }
        }

        stage('End Build') {
            steps {
                echo '构建完成！'
            }
        }
    }
}
